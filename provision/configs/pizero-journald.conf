# /etc/systemd/journald.conf.d/pizero.conf
# Keep journal in RAM only — no constant SD card writes.
# Logs survive between reboots only if log2ram is active.

[Journal]
Storage=volatile
Compress=yes
RuntimeMaxUse=8M
RateLimitIntervalSec=30s
RateLimitBurst=1000
ForwardToSyslog=no
Audit=no
