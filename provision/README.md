# provision/

OS-level setup and hardening for the Pi Zero 2, merged in from the
`PiZero2Installation` repo in **Phase 2**. Will hold the `scripts/`, `configs/`,
`systemd/`, and `templates/` that turn a fresh Raspberry Pi OS into the base this
playground runs on (sysctl/journald tuning, earlyoom, wifi-watchdog, hotspot,
headless + sshd config), plus per-app systemd units.

_Placeholder — populated in Phase 2._
