[package]
name = "devices"
version = "0.1.0"
edition = "2021"

# Shared drivers for the Pi Zero 2's physical peripherals on the breadboard:
# OLED (SH1106), LED, button, PIR motion sensor. App-agnostic.

[dependencies]
rppal = { workspace = true }
sh1106 = { workspace = true }
embedded-graphics = { workspace = true }
linux-embedded-hal = { workspace = true }
