[package]
name = "net"
version.workspace = true
edition.workspace = true

# Small networking helpers shared across apps. Pure std — no dependencies, so
# its unit tests run on any host (not just the Pi).

[dependencies]

[lints]
workspace = true
