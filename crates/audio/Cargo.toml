[package]
name = "audio"
version = "0.1.0"
edition = "2021"

# Microphone capture (and, later, playback) for any app in the workspace.
# Wraps cpal; on the Pi that's the USB sound card via ALSA.

[dependencies]
cpal = { workspace = true }
