[package]
name = "clapper"
version.workspace = true
edition.workspace = true

[[bin]]
name = "clapper"
path = "src/main.rs"

[dependencies]
# Shared workspace crates
devices = { path = "../../crates/devices" } # sound sensor (D0), LED, OLED
net = { path = "../../crates/net" }         # wake-on-lan
appconfig = { workspace = true }            # runtime .env / env-var config

[lints]
workspace = true
