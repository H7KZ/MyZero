# Copy to .env (gitignored) and fill in real values.
# Values are baked into the binary at compile time via build.rs.

# Wake word (trained locally with rustpotter; language-independent)
WAKE_WORD=Jarvis

# Wake-on-LAN target: the Windows PC to boot
WOL_TARGET_MAC=AA-BB-CC-DD-EE-FF
WOL_BROADCAST_ADDR=255.255.255.255
WOL_PORT=9
